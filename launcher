#!/usr/bin/env python3
"""os8-launcher CLI entry point."""

import sys
from pathlib import Path

repo_root = Path(__file__).resolve().parent
sys.path.insert(0, str(repo_root))

from src.cli import main

main(repo_root)
