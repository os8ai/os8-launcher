name: tts
install_type: container
image: python:3.12-alpine
port: 9002
run: "docker run -p {port}:8080 -v {repo_root}/clients/tts/static:/srv:ro {image} python3 -m http.server 8080 --directory /srv"
update: "docker pull {image}"
